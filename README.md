# Система районирования территории Беларуси по солнечной радиации

Геоинформационная система для анализа данных о солнечной радиации с интеграцией OpenStreetMap и поддержкой Laravel backend.

## Возможности

- 🗺️ **Интерактивная карта OpenStreetMap** с маркерами регионов
- 📊 **Визуализация данных** через графики и диаграммы
- 🎨 **Цветовое кодирование** зон по уровню солнечной радиации
- 📈 **Сравнительный анализ** регионов
- 🌓 **Темная/светлая тема** интерфейса
- 🔄 **Интеграция с Laravel API**

## Технологии

- React 18 + TypeScript
- Vite
- Tailwind CSS
- Leaflet / OpenStreetMap
- Recharts
- shadcn/ui

## Установка

### Разработка

```bash
# Установка зависимостей
npm install

# Копирование environment variables
cp .env.example .env

# Запуск dev сервера
npm run dev
```

### Сборка для продакшена

```bash
npm run build
```

## Интеграция с Laravel

См. подробную инструкцию в [LARAVEL_INTEGRATION.md](./LARAVEL_INTEGRATION.md)

### Быстрый старт

1. Соберите React приложение:
```bash
npm run build
```

2. Скопируйте файлы в Laravel:
```bash
cp -r dist/* ../laravel-project/public/solar-app/
```

3. Настройте API endpoints в Laravel (см. документацию)

## Конфигурация

### Environment Variables

```env
# API Base URL для Laravel backend
VITE_API_BASE_URL=http://localhost:8000/api

# Base path для routing
VITE_BASE_PATH=/

# Название приложения
VITE_APP_NAME="Солнечная радиация Беларуси"
```

## Структура проекта

```
src/
├── components/         # React компоненты
│   ├── ui/            # UI компоненты (shadcn)
│   ├── BelarusMap.tsx # Схематичная карта
│   ├── OpenStreetMap.tsx # OSM карта
│   ├── MapTabs.tsx    # Переключение карт
│   └── ...
├── lib/               # Утилиты и helpers
│   ├── api.ts         # API клиент для Laravel
│   └── solar-data.ts  # Данные о регионах
├── types/             # TypeScript типы
└── App.tsx            # Главный компонент
```

## API Endpoints (Laravel)

```
GET /api/solar/regions              # Получить все регионы
GET /api/solar/regions/{id}         # Получить конкретный регион
GET /api/solar/monthly?region={id}  # Месячные данные
GET /api/solar/comparison           # Сравнительные данные
```

## Разработка

### Добавление нового региона

Отредактируйте `src/lib/solar-data.ts`:

```typescript
{
  id: 'new-region',
  name: 'Название региона',
  centerLat: 53.9,
  centerLon: 27.56,
  area: 10000,
  solarRadiation: 1050,
  averageAnnualSunshine: 1780,
  optimalPanelAngle: 35,
}
```

### Кастомизация дизайна

Отредактируйте цветовую схему в `src/index.css`:

```css
:root {
  --solar-low: 45 100% 70%;
  --solar-medium: 30 95% 60%;
  --solar-high: 15 90% 55%;
  --solar-very-high: 0 80% 50%;
}
```

## Производительность

- Код-сплиттинг через React.lazy
- Оптимизация изображений
- Tree-shaking неиспользуемого кода
- Минификация CSS и JS
- Генерация манифеста для Laravel

## Лицензия

MIT

## Контакты

Для вопросов и предложений создавайте issue в репозитории.
